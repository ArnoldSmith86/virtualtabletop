# Domino pip images

The pip images in this directory are by LawDawg96 and are released into the
public domain under [CC0](https://creativecommons.org/publicdomain/zero/1.0/).

Each file `<n>.svg` shows the pip arrangement for the number `n`; a domino half
with zero pips uses no image at all.
